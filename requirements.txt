# Core dependencies required to run the plugin scripts and hooks
biopython>=1.81
numpy>=1.23.0
pyyaml>=6.0
pytest>=7.0.0

# Optional / tool-specific dependencies (install in the target tool environments, not necessarily here):
#   torch -- required by ESM-IF1, ESMFold, ProteinMPNN helper scripts
#   fair-esm -- required by ESM-IF1 / ESMFold wrappers
#   omegafold -- required by OmegaFold wrapper
#   boltz -- required by Boltz-1 wrapper
#   chai1 -- required by Chai-1 wrapper
#   protenix -- required by Protenix wrapper
#   openfold -- required by OpenFold3 wrapper
#   colabfold -- required by ColabFold wrapper
#   ligandmpnn -- required by LigandMPNN wrapper
